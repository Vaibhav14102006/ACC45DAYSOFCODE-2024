#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    cin >> T;  // Read the number of test cases
    while (T--) {
        int N;
        cin >> N;  // Read the weight of pulp for this test case
        cout << 10 * N << endl;  // Output the number of notebooks
    }
    return 0;
}